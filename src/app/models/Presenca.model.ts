
export interface PresencaRequest {
    nome: string;
    familia: boolean;
    confirmacao: boolean;
}

export interface Presenca {
    id: number;
    nome: string;
    familia: boolean;
    confirmacao: boolean;
}